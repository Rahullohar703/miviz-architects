import SEOPage from '../SEOPage';
import { topInteriorDesignersInPuneData } from '../../data/seo-top-interior-designers-in-pune';

export default function TopInteriorDesignersInPunePage() {
  return <SEOPage data={topInteriorDesignersInPuneData} />;
}
