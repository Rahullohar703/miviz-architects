import SEOPage from '../SEOPage';
import { homeInteriorDesignerPuneData } from '../../data/seo-home-interior-designer-pune';

export default function HomeInteriorDesignerPunePage() {
  return <SEOPage data={homeInteriorDesignerPuneData} />;
}
