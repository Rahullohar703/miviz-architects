import SEOPage from '../SEOPage';
import { threeInteriorStylesData } from '../../data/seo-3-interior-styles-never-heard-of';

export default function ThreeInteriorStylesPage() {
  return <SEOPage data={threeInteriorStylesData} />;
}
