import SEOPage from '../SEOPage';
import { bestInteriorDesignerInPuneData } from '../../data/seo-best-interior-designer-in-pune';

export default function BestInteriorDesignerInPunePage() {
  return <SEOPage data={bestInteriorDesignerInPuneData} />;
}
