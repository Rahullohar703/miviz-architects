import SEOPage from '../SEOPage';
import { turnkeyArchitecturePuneData } from '../../data/seo-turnkey-architecture-pune';

export default function TurnkeyArchitecturePunePage() {
  return <SEOPage data={turnkeyArchitecturePuneData} />;
}
