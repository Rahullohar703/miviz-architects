import SEOPage from '../SEOPage';
import { thingsToMakeVillaPremiumData } from '../../data/seo-3-things-to-make-villa-premium';

export default function ThingsToMakeVillaPremiumPage() {
  return <SEOPage data={thingsToMakeVillaPremiumData} />;
}
