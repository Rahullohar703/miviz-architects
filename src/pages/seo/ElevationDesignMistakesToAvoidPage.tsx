import SEOPage from '../SEOPage';
import { elevationDesignMistakesToAvoidData } from '../../data/seo-elevation-design-mistakes-to-avoid';

export default function ElevationDesignMistakesToAvoidPage() {
  return <SEOPage data={elevationDesignMistakesToAvoidData} />;
}
