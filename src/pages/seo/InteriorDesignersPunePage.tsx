import SEOPage from '../SEOPage';
import { interiorDesignersPuneData } from '../../data/seo-interior-designers-pune';

export default function InteriorDesignersPunePage() {
  return <SEOPage data={interiorDesignersPuneData} />;
}
